[project]
name = "daily-industrial-automation-factory"
version = "0.1.0"
description = "GitHub için güvenli günlük TwinCAT ve PLC eğitim projesi yayıncısı"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
target-version = "py311"
